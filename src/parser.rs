use tokenizer;


pub enum Node {
    Empty
}

pub fn parse(it: &tokenizer::TokenIterator) -> Node {
    // TODO: Make a stack-based parser thingy!
    // (Or recursive descent? IDK let's look at it)
    unimplemented!();
}
